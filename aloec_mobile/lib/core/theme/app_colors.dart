import 'package:flutter/material.dart';

class AppColors {
  // Colores principales (Estética saludable y premium)
  static const Color primaryGreen = Color(0xFF2E7D32); // Verde saludable
  static const Color accentGreen = Color(0xFF81C784); // Verde claro de acento
  
  // Colores de fondo y superficies
  static const Color backgroundLight = Color(0xFFF9FBFA); // Blanco verdoso muy sutil
  static const Color surface = Color(0xFFFFFFFF);
  
  // Colores de texto
  static const Color textDark = Color(0xFF212121); // Gris casi negro para lectura
  static const Color textMedium = Color(0xFF666666); // Gris medio
  static const Color textLight = Color(0xFF9E9E9E); // Gris claro
  
  // Colores de estado y utilidad
  static const Color error = Color(0xFFD32F2F); // Rojo error
  static const Color success = Color(0xFF388E3C); // Verde éxito
  static const Color warning = Color(0xFFF57C00); // Naranja advertencia
  
  // Genéricos
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
}
